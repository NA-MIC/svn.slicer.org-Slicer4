PROJECT( Applications )

SUBDIRS(
  CLI
  GUI
)

FIND_FILE(SIGN_DIR SIGN
${CMAKE_CURRENT_SOURCE_DIR}
 )

IF(SIGN_DIR)
SET(SIGN_LIB_ROOT_DIR ${CMAKE_CURRENT_SOURCE_DIR}/../Libs/SIGN)
SUBDIRS(
SIGN
)
ENDIF(SIGN_DIR)


IF (Slicer3_BUILD_TESTING)
    SUBDIRS( Testing )
ENDIF (Slicer3_BUILD_TESTING)
